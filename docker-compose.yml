version: "2"
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    depends_on: 
      - "db"
    ports:
      - "3000:3000"
  db:
    image: mongo
    expose:
      - "27017"
    ports: 
      - "27017:27017"